# Deep-Dive Pages

One file per dimension (`d1.md` through `d13.md`), narrative-style content for `aimaturitymodels.com`'s Per-Dimension Deep-Dive pages — full prose walking through where an organization starts, where the real gains happen, what full maturity looks like, and why the dimension matters. This is the "bonus material" layer beyond the Whole-Model View's terse per-cell summary (`../short_form.yml`).

Sourced from this model's own locked content (`ai_native_sdlc_maturity_model.md`, including the transition/verification notes now folded into it, 2026-07-27) and from `../shared_intelligence_layer.md` for D1–D3. One file (`d11.md`) still carries a visible note naming an open review item from this model's own "Review flags before lock" list — shipped labeled, per David's decision to ship now rather than wait for those flags to clear. `d6.md` and `d9.md` carried the same kind of note originally; both underlying items were reviewed and resolved 2026-07-24 (see `README.md`'s Open Items section), and their notes were updated accordingly rather than left stale.

Section headers throughout (`Where most organizations start`, `Where the real gains happen`, `What the top of the curve actually looks like`) are tagged with the family-wide maturity-level names (Nascent / Modeled → Integral / Telemetric, 2026-07-27) for consistency with the same vocabulary used on aimaturitymodels.com's Whole-Model View and Assessment pages.

Not independently versioned — regenerate from the matrix/shared-layer sources if they diverge, same discipline as `short_form.yml`.
